int a[10],b[10],c[10],n,m,i
printf ("enter number of numbers in first array\n");
scanf ("%d",&n);//first array reading
for(i=0;i<n;i++)
    scanf("%d",&a[i]);

    printf("enter number of numbers in second array\n");
    scanf("%d",&m);//second array reading
    for(i=0;i<m;i++)
    scanf("%d",&b[i]);

  if (m==n)
  {
      \\second array reading
      for(i=0;i<m;i++)
        c(i)=a(i)+b(i);
      printf("sum of tyhe given array  elements\n");
      for(i=0;i<n;i++);

  }
  else
    printf("cannot add");
}
